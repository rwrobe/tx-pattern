# Tx Pattern

This repo provides a demo of a layered architecture that splits the repository layer while also giving the service layer
the ability to manage transactions in an implementation-agnostic way.